# Development backend target for Vite proxy.
# Frontend calls /api/* and Vite forwards it here.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# Localhost is a browser-secure context for camera access, so HTTP is safer
# than a self-signed HTTPS certificate during local development.
VITE_DEV_HTTPS=false
